#define _CRT_SECURE_NO_WARNINGS
             »”„ «·À«·ÊÀ «·«ﬁœ”

#includeiostream	
#includealgorithm
#includecstdio
#includesstream
#includecmath
#includeclimits
#includestring
#includeiomanip
#includevector
#includestack
#includemap
#includequeue
#includedeque
#includeset
#includefstream
#includebitset
#include cstring
using namespace std;
const double EPS = 1e-8;
jesus help me.......
void fast()
{
	stdios_basesync_with_stdio(0);
	cin.tie(NULL); cout.tie(NULL);
}
		

bool sortby(const pairint, int&a, const pairint, int&b)
{
	if (a.first = b.first&&a.second = b.second)
		return true;
	return false;
}



int main()
{
	fast();
	freopen(keys.in, r, stdin);
	don't open standing
	don't open standing
	don't open standing
	don't open standing
	int n,c=-1;
	vectorintv(105);
	int m;
	vectorstringv2(105);
	int j = 0;
	while (cinm&&m)
	{
		int co = 0;
		
		c = max(c, m);
		int i;
		for ( i = 2; ii  m; i++)
		{
			if (m%i == 0)
			{
				co += i;
				co += (m  i);
			}
		}
		if (m!=1)
			co++;
		if (ii == m)co += i;
		if (co == m)v[j] = m, v2[j++] = PERFECT;
		else if (co  m)v[j] = m, v2[j++] = DEFICIENT;
		else v[j] = m, v2[j++]=ABUNDANT;
	}
	cout  PERFECTION OUTPUT  endl;
	int a = 0;
	while (c)
	{
		a++;
		c = 10;
	}
	for (int i = 0; i  j; i++)
	{
		cout  setw(a)  setiosflags(iosright)  v[i]      v2[i]  endl;
	}
	cout  END OF OUTPUT  endl;
	system(pause);
	return 0;
	don't open standing
	don't open standing
	don't open standing
	don't open standing
	don't open standing
	don't open standing
}